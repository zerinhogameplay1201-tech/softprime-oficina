<%- include('partials/header') %>

<h2>Editar Serviço</h2>
<form method="post" action="/services/<%= service._id %>/update">
  <div class="mb-3">
    <label class="form-label">Descrição</label>
    <input name="description" class="form-control" value="<%= service.description %>"/>
  </div>
  <div class="mb-3">
    <label class="form-label">Preço (R$)</label>
    <input name="price" type="number" step="0.01" class="form-control" value="<%= service.price %>"/>
  </div>
  <div class="mb-3">
    <label class="form-label">Duração estimada (horas)</label>
    <input name="duration" type="number" step="0.1" class="form-control" value="<%= service.duration || '' %>"/>
  </div>
  <div class="mb-3">
    <label class="form-label">Custo com peças (R$)</label>
    <input name="parts_cost" type="number" step="0.01" class="form-control" value="<%= service.parts_cost || 0 %>"/>
  </div>
  <button class="btn btn-primary">Salvar</button>
  <a class="btn btn-link" href="/services">Cancelar</a>
</form>

<%- include('partials/footer') %>